
操作系统的一个关键需求是同时支持多个进程。例如，一个进程可以使用第一章中的`fork`系统调用创建一个新的进程。操作系统必须在进程之间时分复用有限的计算机资源。例如，即使进程比 $\text{CPU}$（单核）的数量要多时，操作系统也必须确保所有的进程都有机会执行。操作系统还必须安排好进程之间的隔离。也就是说，如果一个进程有 bug 或故障，它不应该对那些与这个有 bug 或故障的进程无关的进程产生影响。但是，完全的隔离又太过了，因为进程之间有时需要进行交互；管道就是一个例子。因此，一个操作系统必须满足以下三个要求：多路复用、隔离和交互。